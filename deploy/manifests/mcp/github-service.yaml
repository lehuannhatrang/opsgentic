apiVersion: v1
kind: Service
metadata:
  name: github-mcp
  namespace: opsgentic
spec:
  selector:
    app: github-mcp
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      nodePort: 31082
  type: NodePort

# 2026-06-30

## demo-down.sh — safer teardown

- Strip ArgoCD Application finalizers (`resources-finalizer.argocd.argoproj.io`) before deleting,
  so the `argocd` namespace no longer gets stuck in `Terminating`.
- Stop deleting the `argocd`/`prometheus` namespaces by default — they may pre-exist the demo
  (e.g. an ArgoCD the user already ran). Only `helm uninstall` runs by default; namespace
  deletion is opt-in via `--purge-namespaces` or an interactive prompt (which also clears any
  leftover Application finalizers first).

## demo-up.sh — GitHub PAT write-permission guard

- Clarified the PAT instructions to warn against "Public repositories (read-only)" access, which
  grants read only and makes PR creation 403 at runtime.
- After resolving the fork, verify `.permissions.push == true` on the repo and fail fast with a
  clear message if the token cannot write (was surfacing only later as a 403 on `POST git/refs`).

## README — SEO + website link

- Added the GitHub Pages site link (https://lehuannhatrang.github.io/opsgentic/) as a badge, a nav
  row, a Documentation-table row, and in Contributing.
- SEO polish: keyword-rich intro paragraph, descriptive image alt text, a new FAQ section for
  long-tail queries, and an expanded keywords footer.

## Console Graph Visualize

- Added `opsgentic/agent_registry.py` centralizing agent→MCP-server and
  agent→graph-node mappings; refactored `mcp/context.py` and
  `conversation/responder.py` to consume it.
- Added `opsgentic/graphview.py`: `build_system_graph()` (static topology via
  `app.get_graph()` + registry + skills + MCP servers), `build_run_graph()` and
  `executed_steps()` (per-run trace from `get_state_history()` +
  `context_data.tool_calls`), and `list_server_tools()` (lazy MCP tool probe).
- New API endpoints: `GET /graph`, `GET /graph/tools/{server}`,
  `GET /runs/{thread_id}/graph`; proxied by the console BFF.
- Console: new Graph tab (System + Run modes) rendering with vendored
  Cytoscape.js — agents/servers/skills/memory nodes, conditional flow edges,
  lazy tool expansion, and run-trace overlay (ran/current/pending + tool badges).
- Added `tests/` (pytest) covering the registry and both graph builders
  (10 tests, all passing).
